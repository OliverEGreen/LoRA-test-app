# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# Per-style experiment folders (training data, outputs, weights — kept local).
# Everything actually shippable lives in space/.
Akira/
Kodachrome/

# Conventional artefact subdirs in case future experiments re-introduce them
# without being whole-folder-ignored above:
_Generated Outputs/
_Safetensor Weights/
Training Data/

# LoRA weights — distributed via the HF Space, not GitHub
*.safetensors
